import React from 'react';
import styles from './Example.scss';

const Example = () => {
    return (
	    <div className={styles.contain}>
	        <a href="">我只是个Example</a>
	    </div>
    );
};

Example.propTypes = {
	
};

export default Example;
